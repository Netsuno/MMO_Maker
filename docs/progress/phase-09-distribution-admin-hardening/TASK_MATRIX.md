# Phase 9 — Task matrix

Statuses: `DONE` | `IN PROGRESS` | `NOT STARTED` | `DEFERRED`.  
Owners are **roles**, not agent IDs.

| Task id | Owner agent role | Files / modules (current or expected) | Tests required | Acceptance criteria | Status |
| --- | --- | --- | --- | --- | --- |
| **P9-0** | architecture/planning + documentation-release-reviewer | `docs/progress/phase-09-distribution-admin-hardening/*`, `docs/STATUS.md` | None (docs). Cite real paths only. | Folder exists with PHASE_PLAN, TASK_MATRIX, BASELINE_AUDIT, stub shells. STATUS says Phase 8 ACCEPTED on main and Phase 9 IN PROGRESS. P9-S decided. | **DONE** |
| **P9-1** | server-engineer + security | `Frog.Server/Services/AdminCommandService.cs` (stub today), `Frog.Server/Models/Role.cs`, `Permission.cs` (stubs), `Frog.Core/Enums/AccessRightEnum.cs` (stub), `Frog.Server/Network/PacketDispatcher.cs`, `Frog.Core/Enums/PacketId.cs`, `Frog.Core/Enums/ChatChannel.cs`, `Frog.Server/Security/ChatRateLimiter.cs`, `Frog.Client/Network/FrogGameClient.cs`, `Frog.Client/MainShellForm.cs`, new PG entities/migrations under `Frog.Persistence.PostgreSql/` | Unit: mute/kick/ban authorization + persistence. PG: ban survives restart. TCP: muted player cannot send `ChatSend`; kicked session closed; banned login/reconnect rejected. Regression: Phase 7 chat Global/Map/Whisper still works for non-sanctioned players. | Operator can mute, kick, and ban. Actions persist in PostgreSQL. Enforcement is server-authoritative. No client-only “admin”. No guild/group/trade work. | **NOT STARTED** |
| **P9-2** | security-engineer | `docs/progress/phase-09-distribution-admin-hardening/SECURITY_MODEL.md`, `Frog.Core/Security/PasswordHasher.cs`, `Frog.Application/Identity/AccountInputRules.cs`, `Frog.Application/Identity/IOperatorDirectory.cs`, `Frog.Persistence.PostgreSql/Repositories/Auth/PostgresOperatorDirectory.cs`, `Frog.Persistence.PostgreSql/Migrations/20260917223000_AuthOperators.cs`, `Frog.Server/appsettings.json`, `Frog.Server/appsettings.Local.json.example`, `Frog.Server/Network/PacketDispatcher.cs` (`WorldFlagsPatchRequest`), `Frog.Server/Security/WorldFlagsPatchPolicy.cs`, `Frog.Server/Security/PlaceholderSecretPolicy.cs`, `Frog.Server/Security/LoginRateLimiter.cs`, `Frog.Server/Config/ServerOptions.cs`, `docs/progress/phase-08-quests-events-advanced-creation/COMMAND_CATALOG.md` | Auth: PBKDF2 still used; timing-safe reject still holds. Negative: unprivileged account is not an operator (no mute/kick/ban opcodes yet). `WorldFlagsPatchRequest` rejected in PG production and production composition. Secret files stay gitignored. Bind non-loopback requires explicit flag. | Written threat model. Permission SoT = `auth.operators` (not an account flag). Dangerous leftover packets listed. Bind/TLS/secret-handling recorded; safe gates implemented. Mute/kick/ban product deferred to P9-1. | **DONE** |
| **P9-3** | postgresql-engineer | `scripts/postgres-backup.sh` (+ `.ps1`), `scripts/postgres-restore.sh` (+ `.ps1`), `scripts/postgres-verify.sh` (+ `.ps1`), `scripts/postgres-backup-restore-smoke.sh`, `scripts/postgres-common.sh`, `BACKUP_RESTORE_RUNBOOK.md`, `tests/Frog.Persistence.IntegrationTests/PostgresBackupRestoreTests.cs`, `.github/workflows/ci.yml` (`postgresql-client` on `postgres-integration`). Migrations read-only (tip `20260917223000_AuthOperators`, includes P9-2 `auth.operators`). | Integration: empty DB migrates; `pg_dump` of a seeded Phase 7 world restores; `PostgresDatabaseHealth` OK (0 pending); Phase 7 TCP login + register against restored DB. Filter `FullyQualifiedName~.PostgresBackupRestoreTests`. Re-run after P9-1 migrations. | Documented `pg_dump`/`pg_restore` against schemas `auth`, `content`, `ops`, `player`, `world`. Restore proven on a disposable database. No new MariaDB path. | **DONE** |
| **P9-4** | release-operations-engineer | `scripts/publish-frog.sh` (+ `.ps1`), `scripts/run-packaged-server.sh` (+ `.ps1`), `scripts/packaged-server-smoke.sh`, `Frog.Server/Build/CopyPostgreSqlRuntime.targets` (portable PG sidecar; do not forward host RID), `Frog.Server/Frog.Server.csproj` (`CopyToPublishDirectory=Never` for Local overlay), `.github/workflows/ci.yml` (layout smoke on existing `postgres-integration` job), `PACKAGING_GUIDE.md`, `OPERATIONS_RUNBOOK.md` | Existing `PackagedServerPostgreSqlProcessTests` now publishes via `publish-frog.sh` on Linux (RID layout + PG login/shop). Layout: `./scripts/packaged-server-smoke.sh --layout-only`. Client/editor gate remains Windows smokes (do not weaken Phase 8 SHA). | Repeatable publish layout for server (Linux + Windows) and WinForms client/editor (Windows). Config overlay documented (`appsettings.json` + gitignored `appsettings.Local.json`). No `.fcc` importer, no MariaDB requirement. | **DONE** |
| **P9-5** | performance-engineer | `Frog.Server/Network/ClientSession.cs` (1 MiB frame cap), `Frog.Server/Services/MovementPacketRateGate.cs`, `Frog.Server/Security/ChatRateLimiter.cs`, `Frog.Core/Gameplay/GameplayLimits.cs`, `Frog.Core/Constants/WorldMetrics.cs`, `LOAD_REPORT.md` | Load harness (new) + existing TCP/PG tests as sanity. Do not replace Phase 8 E2E. | Proposed thresholds in BASELINE_AUDIT are **measured or explicitly revised**. Enough logs/metrics to see connections, rejects, and PG errors. UDP/AOI not built. | **NOT STARTED** |
| **P9-6** | qa-ci-reviewer + documentation-release-reviewer (prefer read-only on product code) | `TEST_PLAN.md`, `E2E_MATRIX.md`, `PHASE_REPORT.md`, `REVIEW_REQUEST.md`, `KNOWN_ISSUES.md`, `CHANGE_SUMMARY.md`, `docs/STATUS.md` | Full CI suite as on main: Frog.Tests, PG integration, Editor / Gameplay / Phase 8 smokes ×3. Plus P9-1…P9-5 tests. | Evidence pins real SHAs and real CI run URLs. No invented CI. Phase 8 regressions listed if found. Phase 9 READY/not READY is explicit. | **NOT STARTED** |
| **P9-S** | architecture/planning (decision only) | N/A — no product files | None | Remain **DEFERRED**. See PHASE_PLAN citation. Historical `Guild*.cs` stubs stay unused. | **DEFERRED** |

## Notes for later owners

- documentation-release-reviewer should prefer editing this folder and STATUS, not product behavior.
- qa-ci-reviewer should not weaken Phase 8 screenshot SHA gates (`scripts/verify-phase8-screenshot-manifest.ps1`).
- P9-1 must not reuse MariaDB `accounts` / admin folklore. New tables go through EF migrations in `Frog.Persistence.PostgreSql`.
- `docs/DATA_MODEL.md` is stale (maps/tilesets only). Refresh it under P9-3 or P9-6 documentation, not as a silent schema change.
